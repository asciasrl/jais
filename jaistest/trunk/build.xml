<project default="jar">
	<target name="compile">
		<javac srcdir="src" />
		<javac dstdir="bin" />
	</target>

	<target name="jar">
		<delete file="JaisTest.jar"/>
		<property name="version.num" value="1.00"/>
		<buildnumber file="build.num"/>
		<tstamp>
			<format property="TODAY" pattern="yyyy-MM-dd HH:mm:ss" />
			<format property="YMDHMS" pattern="yyyyMMddHHmmSS" />
		</tstamp>
		<property name="v" value="${version.num}.v${YMDHMS}-b${build.number}"/>
		
		<manifest file="MANIFEST.MF">
			<attribute name="Built-By" value="${user.name}"/>
			<attribute name="Main-Class" value="BusTest"/>
			<attribute name="Implementation-Version" value="${version.num}-b${build.number}"/>   
			<attribute name="Built-Date" value="${TODAY}"/>                 
		</manifest>
		
		<jar destfile="JaisTest.jar"
		   basedir="bin"
		   includes="**/*.class"
		   manifest="MANIFEST.MF"
		   />
		<delete verbose="true">
			<fileset dir="../installer/lib">
			  <include name="JaisTest*.jar"/>
			</fileset>
		</delete>
		<copy verbose="true" file="JaisTest.jar" tofile="../installer/lib/JaisTest-${v}.jar" />
		<copy verbose="true" todir="../installer/conf" >
			<fileset dir="conf">
			  <include name="*.xml"/>
			</fileset>
		</copy>
	</target>
	
	<target name="cleanup">
	 <delete>
		 <fileset dir="." includes="**/*.class"/>
		 <fileset file="MANIFEST.MF"/>
	 </delete>  
	</target>
	
	<target name="build" depends="compile,jar,cleanup" />
</project>
  